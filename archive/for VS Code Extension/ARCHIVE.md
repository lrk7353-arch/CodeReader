# Archived Direction: VS Code Extension

This folder preserves the earlier VS Code Extension-oriented CodeReader documents for reference only.

This is no longer the MVP or primary product direction. The active route is documented in `../../doc for app/` and defines CodeReader as an independent desktop AI code reading IDE.
