# Activity board

A small browser application used to validate CodeReader's project-first reading flow. The entry
loads activities through the API adapter, stores them, and renders the resulting list.
