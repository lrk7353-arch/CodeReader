# Archived Direction: VS Code Extension

This folder preserves the earlier VS Code Extension-oriented CodeReader documents for reference only.

This is no longer the MVP or primary product direction. The superseded standalone-app MVP route is preserved in `../../app-mvp/`; current production direction is documented under `docs/architecture/`.
