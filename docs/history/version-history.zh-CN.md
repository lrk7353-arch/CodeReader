# CodeReader 版本沿革与兼容性说明

[English product information](../../README.md) | [中文使用说明](../../README.zh-CN.md) | [完整变更日志](../../CHANGELOG.md)

本文面向用户、测试人员和发布维护者，说明 CodeReader 从早期原型到 1.0 的功能演进，以及升级时必须遵守的兼容性边界。旧的 MVP、内测、规划记录仍保留在本目录下，供追溯使用；它们不是当前产品行为的权威来源。

## 版本总览

| 版本阶段 | 主要交付 | 对用户的意义 |
| --- | --- | --- |
| 0.1.0 | 桌面阅读器基础外壳 | 建立本地阅读体验的最小基础。 |
| 0.10.0 | 项目文件树、阅读路径、进度、Python/SQL 解释、Windows x64 包装 | 可以对本地代码项目进行连续阅读，并保留阅读状态。 |
| 0.11.0 beta.1 | 事务迁移、模型提供方边界、跨平台质量门禁 | 数据升级和模型调用开始具备可测试的稳定边界。 |
| 0.11.0 beta.2 | 稳定错误码、交互回归测试、诊断与凭据失败处理 | 用户操作失败时能够得到可执行的恢复方向。 |
| 0.11.0 beta.3 | 提示词版本库、灰度、回滚、Linux 验证 | 已保存的提示词和模型工作流具备受控演进能力。 |
| 0.11.0 beta.4 | 脱敏反馈、项目级进度、模型连通性检查、真实项目夹具 | 内测反馈和大项目阅读具备更完整的验证依据。 |
| 1.0.0-rc.1 | 首次生产候选构建尝试 | 安装包 smoke 未通过，未发布任何资产；标签保留用于审计，不可复用。 |
| 1.0.0-rc.2 | 原生授权文件访问、全文件树/安全预览、迁移恢复、跨架构发布链 | 已被后续候选替代；保留标签与记录用于审计。 |
| 1.0.0-rc.3 | 项目优先入口、正交认知状态、分层解释、个人记录、复查队列 | 已形成 draft 并完成 package smoke；保留为历史事实，不向后复用证据。 |
| 1.0.0-rc.4 | 四平台可重复原生产品旅程 harness 候选 | 发布运行在 validate 阶段失败，未生成安装包；保留 tag 和运行用于审计。 |
| 1.0.0-rc.5 | 发布检出修复候选 | 三个平台 package smoke 通过；Windows x64 清理竞态失败，未汇总最终证据。 |
| 1.0.0-rc.6 | Windows smoke 清理竞态修复候选 | 当前候选；从零执行四平台构建、原生旅程与最终发布证据。 |

## 0.10.x 到 0.11.x：阅读工作流建立

0.10.x 引入真实目录层级、安全文本预览、阅读路径和文件级进度，并扩展 Python、SQL 的结构化解释。它还建立了 Windows x64 的早期 NSIS/MSI 包装和恢复验证。

0.11.x 内测阶段没有改变“本地优先、用户选择文件后才访问”的方向，而是把这一方向变成可维护的系统能力：

- 迁移使用 SQLite 版本控制与事务边界。
- 模型调用、解释上下文、错误分类和前端工作流被拆分为可测试的模块。
- 提示词支持登记、灰度和回滚，已有模型配置与凭据引用是兼容性义务。
- 阅读进度、反馈和诊断在保留隐私边界的前提下持续完善。

详细的 beta 验收、真实项目验证和历史设计资料保留在本目录的 beta 子目录。

## 1.0.0-rc.2：从内测能力到生产候选

1.0 候选版将项目定位从 MVP/内测转为受维护的本地桌面产品。rc.1 的首次构建尝试在安装包 smoke 阶段失败，不曾发布资产；rc.2 修复后作为新标签重新验证：

- 文件或目录只能由原生选择器授予，界面层不获得任意路径或网络权限。
- 用户可以打开任意有权限的位置；树中保留普通文件，主阅读区只渲染可安全支持的格式。
- Markdown 成为一等阅读格式；图片预览、文本/代码预览都具有边界；不支持、二进制或过大文件显示元数据而不替换当前文档。
- 后台扫描、刷新与解释生成绑定到目标身份，旧任务不能覆盖当前文件状态。
- 解释、进度、提示词版本、模型配置和凭据引用延续 0.10.x/0.11.x 的兼容义务。
- 数据库升级先验证备份，再在单一事务中迁移并进行完整性检查；无法安全恢复时进入非破坏性恢复状态。
- 发布矩阵覆盖 Windows/Linux 的 x64、ARM64，生成十个安装包以及校验和、SPDX SBOM、artifact attestations 和原生 smoke 证据。

## 1.0.0-rc.3：产品复位候选

rc.3 把维护者已在三类真实项目上验收通过的产品复位体验纳入新的不可变候选版本。主要变化包括项目优先入口、访问/掌握/复查正交状态、项目到局部代码的分层解释、个人标注与阅读偏好、相关代码往返、代码变化复查，以及 Prompt 和 Markdown Outline 的通用可用性修复。

该版本仍受 R5 发布门禁约束。Windows/Linux x64/ARM64 的十个安装包、四份 package smoke、四份完整原生产品旅程、最终校验和/SPDX/证明和维护者公开发布批准必须基于 rc.3 的同一 commit 与 tag 重新取得。rc.2 的制品或历史证据不能移用于 rc.3。

R4 的三类真实项目人工使用验收已经完成并通过。当前未完成范围仅是 R5：Windows/Linux x64/ARM64 的原生安装包、package smoke、完整产品旅程及最终发布证据；这些门禁通过并取得维护者公开发布批准前，rc.3 仍不是稳定版或公开发布。

## 1.0.0-rc.4：原生旅程 harness 候选

rc.4 将经独立监督的 Windows/Linux 原生旅程驱动纳入新候选。Linux 与 Windows harness 的静态候选均已通过 Sol `high` 监督，但四个平台尚未基于 rc.4 tag 实际执行，不能写成四平台旅程已通过。R4 small、frontend、fullstack 三项目的维护者人工可用性已经通过；当前仅 rc.4 的四平台 OS 级 native journey 与最终发布证据未完成。

`rc.3` draft 已完成的十包和四份 package smoke 只属于 rc.3，不复用于 rc.4。rc.4 必须从同一新 commit/tag 重新生成十包、四份 package smoke、四份 native journey、`SHA256SUMS`、SPDX SBOM 与 artifact attestations；取得这些证据及维护者发布批准前，R5 仍未 `PASS`。

rc.4 的 Production Release run `31654257774` 在 validate 阶段因浅检出缺少固定历史 schema 对象而失败。该运行未启动四平台构建，未生成安装包、package smoke、native journey 或最终发布证据；rc.4 tag 与失败运行保留用于审计，但任何结果均不得复用于后续候选。

## 1.0.0-rc.5：发布检出修复候选

rc.5 修正发布验证所需的历史对象检出边界，并以新的不可变 commit/tag 从零执行。它原计划重新生成十包、四份 package smoke、四份 native journey、`SHA256SUMS`、SPDX SBOM 与 artifact attestations，不继承 rc.4 的失败运行；实际执行结果如下。

Production Release run `31656328041` 实际通过 validate、Linux x64、Linux ARM64 与 Windows ARM64 package smoke。Windows x64 的 NSIS/MSI 构建成功，但 smoke 清理阶段在进程自行退出后调用 `Stop-Process`，触发竞态并失败；verify 与 assemble 因依赖失败而跳过，未生成 draft 或最终证据。rc.5 的 tag、制品和单平台 smoke 仅作为审计事实保留，不向后续候选复用。

## 1.0.0-rc.6：Windows smoke 清理竞态修复候选

rc.6 修复 Windows package smoke 清理已退出进程时的竞态。它必须基于新的同一不可变 commit/tag 从零生成十包、四份 package smoke、四份 native journey、`SHA256SUMS`、SPDX SBOM 与 artifact attestations；不得继承 rc.5 的安装包或三个已通过平台的 smoke。R4 维护者人工可用性验收仍已通过；rc.6 四平台 OS 级 native journey 与最终发布证据完成前，R5 仍未 `PASS`。

## 升级与数据兼容性

### 受支持的升级来源

- 0.10.x：CodeReader 会发现受支持的旧应用数据位置，只有在当前数据库不存在时才导入；绝不会用旧数据库覆盖已存在的当前数据。
- 0.11.x：沿用已保存的解释、阅读进度、提示词版本、模型配置与凭据引用，并通过当前 schema 迁移升级。

### 每次迁移的安全流程

1. 打开旧数据库并执行完整性检查。
2. 创建且验证备份。
3. 在单一事务中应用所需迁移。
4. 执行 schema、外键和数据完整性验证。
5. 成功后打开新数据库；失败时保留原始数据库和恢复路径。

### 不支持的操作

- 不支持在没有备份的情况下将新数据库降级给更早版本。
- 不支持为了跳过恢复状态而手动删除数据库或备份。
- 不承诺对未记录的开发快照、手工篡改数据库或未知未来 schema 的兼容性。

出现恢复状态时，请保留数据库与备份，记录应用版本、系统、稳定错误码和复现条件，再提交脱敏后的 Issue。

## 发布通道与版本号

- 形如 1.0.0-rc.N 的版本是候选版，用于完整生产验证。它可能在发现问题后被后续候选版替代。
- 1.0.0 是稳定版。稳定版不应覆盖或静默替换已发布资产。
- Windows/Linux 首发支持 x64 与 ARM64；macOS 在下一版本规划中。

版本号与安装包、package.json、Cargo、Tauri 配置和 Windows MSI 数值版本必须一致。发布时应以 GitHub Release 页面和随附 SHA256SUMS、SBOM、attestation 为准。
