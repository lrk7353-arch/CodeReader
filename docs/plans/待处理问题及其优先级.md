# 待处理问题及其优先级

**状态：** 维护者已知、已接受延期；不等同于已解决。

本文件只记录已经明确归属、但不会阻断当前复位主线继续推进的问题。任何 P0、数据损坏、隐私泄露、兼容性破坏或黄金路径不可用的问题仍应立即阻断阶段。

## P1：R2 原生辅助验收补全（已转入 R5 原生旅程门禁）

**决定：** 维护者于 2026-08-09 明确接受将本组人工原生验收记录并搁置；R3 可以开始。该决定只豁免 R2 的阶段门禁，不构成产品发布或无障碍完成声明。

**现有证据：**

- 真正的 Linux/WSLg Tauri 与 WebView 已测得 1180 与 760 逻辑窗口下无页面横向溢出、三面板切换存在；
- 进程级 DPR=2 已验证缩放机制和 DOM 溢出边界，但因物理窗口超出测试显示器，不构成完整 200% 视觉验收；
- 局部关键文字对比度样本通过 4.5:1；模型未配置时已有代码与解释仍可读；
- 详见 `docs/plans/2026-08-09-product-reset-r2-evidence.md`。

**尚待补全：**

1. 真实 OS 键盘全流程与可见焦点：路径 → 代码 → 解释 → 原节点；
2. 原生目录选择器、关闭重启、重新授权与位置恢复；
3. `prefers-reduced-motion`；
4. 长内容压力；
5. 完整 200% 视觉与全界面对比度。

**原因与边界：** 当前 WSLg 会话无法可靠地向目标窗口发送原生输入；WebKit Inspector 也没有 Input domain。为避免误操作其他桌面应用，未以不可靠坐标、合成事件、CSS 或 jsdom 代替人工验收。

**R5 处置决定：** 不再继续延期，也不通过缩小支持范围绕过。自动化已经覆盖 `prefers-reduced-motion` 样式、焦点恢复、语义角色、长结构列表、Markdown 长内容布局、窄容器与缩放机制；这些只作为机制证据。真实 OS 键盘/可见焦点、原生选择器、重启重新授权恢复、长内容、200% 视觉和全界面对比度被纳入四个平台的 `native-journey-<platform>-<arch>.json`，必须全部原生观察通过。

**当前状态：R5 原生执行待完成。** Linux 与 Windows native journey harness 的静态候选已经分别通过 Sol `high` 独立监督；该结论关闭了“缺少可信可重复执行方案”的实现问题，但不关闭原生证据门禁。rc.4 的 Production Release run `31654257774` 在 validate 阶段失败，未生成安装包或发布证据；其 tag、失败运行和日志仅用于审计，不得复用。当前 rc.5 仍须在 Windows/Linux x64/ARM64 runner 上从零实际重建并运行；源码审查、静态监督、jsdom、CSS 检查、rc.3 package smoke 或 rc.4 失败运行均不能写成 rc.5 原生旅程通过。四份原生证据缺失时，R5 必须停在发布候选，不得宣称 1.0 或公开发布完成。

**证据协议：** `node scripts/release-evidence.mjs journey-template ...` 生成待填写模板；`verify-journeys` 只接受绑定同一 tag/commit/platform/arch 且全部检查为 `pass` 的四份记录。模板不得写入个人绝对路径、源码、提示词、模型响应、凭据、日志或自由文本备注。

## 已完成：R4 维护者真实使用验收

**结果：** 自动化和仓库自有三类项目样本没有替代维护者对“是否好用”的判断。维护者已完成 small、frontend、fullstack 三类项目人工使用验收并判断整体通过。

**已执行旅程：** 依次在原生应用打开 `examples/small`、`examples/frontend`、`examples/fullstack`，检查入口/核心模块/主路径、推荐路径、问题记录、恢复和代码变化复查。

**统计与证据：** 分母、失败、环境和帮助次数必须逐次保存；不得用单元测试通过率替代恢复成功率、路径完成率、陈旧识别率或主观好用结论。执行模板见 `2026-08-12-product-reset-r4-evidence.md`。

**边界：** 未补写不存在的逐项计时或统计样本；人工发现的两个通用界面问题已转入下方 P2 收口项。

## 已完成：R4 人工验收界面收口

**人工结论：** 维护者已完成 small、frontend、fullstack 三类项目人工验收，整体通过；同时确认以下两个通用可用性问题需要在 R4 收口。

1. Prompt 版本管理在约 503px 窄内容区中，版本、状态、灰度、来源、备注、模板、更新时间和操作互相挤压。
2. README Markdown Preview 的 Outline 固定占用正文空间，不能拖动调整，也不能隐藏后恢复。

**修复候选：** Prompt 表格已增加容器级响应式重排，窄容器显示带字段名的分组记录，保留原表格语义和全部操作；Markdown Outline 已增加鼠标/键盘可调分隔条、明确 ARIA、140–360px 与内容区 45% 限制、隐藏/恢复入口及窄内容区自动收起。

**关闭证据：** Sol medium 完成修复；定向回归覆盖 Prompt 503px/宽屏生产布局、Outline 的 ResizeObserver、鼠标拖动、完整键盘操作及宽度边界。根智能体完整门禁通过（Rust 124 项、前端/脚本 47 files / 329 tests），Sol high 独立复审 `PASS`。机制级回归不冒充完整原生视觉验收。
